name: Trello sync
run-name: Trello • ${{ github.event.action }} • #${{ github.event.issue.number }} ${{ github.event.issue.title }}

on:
  issues:
    types: [opened, edited, labeled, unlabeled, assigned, unassigned, closed, reopened]
  workflow_call:
    secrets:
      TRELLO_API_KEY: { required: true }
      TRELLO_API_TOKEN: { required: true }
      TRELLO_BOARD_ID: { required: true }
      TRELLO_TODO_LIST_ID: { required: true }
      TRELLO_DONE_LIST_ID: { required: true }
      TRELLO_MEMBER_MAP: { required: true }

permissions:
  contents: read

concurrency:
  group: trello-issue-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  sync:
    name: Synchronize issue #${{ github.event.issue.number }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Apply “${{ github.event.action }}” to Trello
        uses: rajipkanxo01/Trello-Github-Sync@main
        env:
          TRELLO_API_KEY: ${{ secrets.TRELLO_API_KEY }}
          TRELLO_API_TOKEN: ${{ secrets.TRELLO_API_TOKEN }}
          TRELLO_BOARD_ID: ${{ secrets.TRELLO_BOARD_ID }}
          TRELLO_TODO_LIST_ID: ${{ secrets.TRELLO_TODO_LIST_ID }}
          TRELLO_DONE_LIST_ID: ${{ secrets.TRELLO_DONE_LIST_ID }}
          TRELLO_MEMBER_MAP: ${{ secrets.TRELLO_MEMBER_MAP }}
